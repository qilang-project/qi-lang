---
name: qi-lang
description: Write, understand, and debug Qi (奇语) programming language code — a compiled language with 100% Chinese keywords. Use when the user asks to write Qi code, create .qi files, understand Qi syntax, fix Qi compilation errors, or work with the Qi standard library. Covers all language features including functions, types, control flow, concurrency, standard library modules (HTTP, JSON, networking, GUI, crypto, etc.), and the LLVM-based compilation pipeline.
metadata:
  author: qilang
  version: "0.2"
---

# Qi 语言 (奇语)

Qi（奇语）是一门 100% 中文关键字的编译型编程语言，通过 LLVM 后端编译为原生可执行文件。

> 相关技能：`qi-web`（Web 框架）、`qi-cli`（命令行框架）、`qi-harness`（LLM Agent 框架）—— 都是用 Qi 写的库，使用它们时先读对应技能。

## 何时使用此技能

- 用户要求编写 `.qi` 文件或 Qi 语言代码
- 用户需要理解、调试或修复 Qi 程序
- 用户提到"气语言"、"中文编程"、"Qi language"
- 用户需要使用 Qi 标准库（HTTP、JSON、网络、GUI、加密等）
- 用户需要理解 Qi 编译器的行为或错误信息

## 基本语法

### 程序结构

每个 Qi 程序必须有包声明和入口函数：

```qi
包 主程序;

函数 入口() {
    打印行("你好，世界！");
}
```

- `包 <名称>;` — 包声明（必须）
- `函数 入口()` — 程序入口点（等价于 main）
- 支持中文分号 `；` 和英文分号 `;`
- 支持中文冒号 `：` 和英文冒号 `:`

### 变量与常量

```qi
变量 名字 = "Qi";           // 可变变量，类型推断
变量 年龄: 整数 = 10;       // 可变变量，显式类型
常量 PI = 3.14159;          // 不可变常量
```

### 基础类型

| 类型名 | 含义 | LLVM 实际类型 |
|--------|------|------|
| `整数` | 整数（**默认 64 位**） | i64 |
| `长整数` | 64位整数 | i64 |
| `短整数` | 16位整数 | i16 |
| `字节` | 无符号8位 | i8 |
| `浮点数` | 64位浮点 | double (f64) |
| `布尔` | 布尔值 | i1 |
| `字符` | 字符 | i8 |
| `字符串` | UTF-8字符串（指针） | ptr |
| `空` | 无返回值 | void |

> ⚠️ `整数` 在 codegen 里映射为 **i64**（不是 i32）。需要更窄类型时用 `短整数`/`字节`。

布尔字面量：`真` / `假`（FFI 布尔常返回整数 `0`/`1`，判断时用 `== 1`）

### 函数

```qi
函数 加法(x: 整数, y: 整数): 整数 {
    返回 x + y;
}

// 无返回值
函数 打招呼(名字: 字符串) {
    打印行("你好，" + 名字);
}
```

### 控制流

```qi
// 条件分支
如果 x > 10 {
    打印行("大于10");
} 否则 {
    打印行("小于等于10");
}

// while 循环
当 计数 < 5 {
    计数 = 计数 + 1;
}

// for 循环
对于 i 在 0..10 {
    打印(i);
}

// 无限循环 + 跳出
循环 {
    如果 条件 { 跳出; }
}
```

### 数组

```qi
变量 数组 = [1, 2, 3, 4, 5];
打印(数组[0]);  // 第一个元素
```

### 自定义类型（结构体）

```qi
类型 点 {
    x: 浮点数,
    y: 浮点数
}

// 方法是 Go 风格：接收者写在函数名前的括号里，不是 `方法 点.距离`
函数 (自己 点) 距离(其他: 点): 浮点数 {
    // 通过 自己.x / 自己.y 访问字段
    返回 自己.x - 其他.x;
}

// 构造结构体两种写法都行：
变量 p = 新建 点 { x: 1.0, y: 2.0 };
变量 q = (点 { x: 3.0, y: 4.0 });   // 括号包裹的结构体字面量（示例里常用）
```

> 结构体字面量里**每个字段单独赋值**，不要把多个字段都从同一个参数取（会触发已知的 codegen 内存踩踏 bug）。

### 错误处理

```qi
尝试 {
    变量 内容 = 读取文件("数据.txt");
} 捕获 错误 {
    打印行("出错了：" + 错误);
} 最终 {
    打印行("清理完成");
}
```

### 匹配（模式匹配）

```qi
匹配 值 {
    1 => 打印行("一"),
    2 => 打印行("二"),
    _ => 打印行("其他"),
}
```

## 关键字完整列表

### 控制流
`如果` `否则` `当` `对于` `循环` `返回` `跳出` `继续` `匹配`

### 声明
`函数` `变量` `常量` `类型` `枚举` `方法` `闭包` `内联` `联合体` `新建`

### 模块
`导入` `导出` `包` `模块` `公开` `私有` `作为` `在` `参数`

### 并发
`启动` `协程` `通道` `选择` `情况` `并发` `未来` `异步`

### 同步原语
`等待组` `互斥锁` `读写锁` `条件变量` `仅一次`

### 错误处理
`尝试` `捕获` `抛出` `最终` `重试` `超时`

### 中文运算符
`等于` `不等于` `大于` `小于` `大于等于` `小于等于` `取余` `加` `减` `乘` `除` `与` `或`

### 指针操作
`解引用` `取地址` `指针` `引用` `可变引用` `自己`

### 保留字地雷（不能做变量名 / 字段名 / 参数名）

这些是 qi 的关键字，写成标识符会报「意外的标记」解析错误。**最常被误用的**：

```
结果  类型  选项  通道  尝试  捕获  抛出  最终  继续  跳出
返回  等待  异步  新建  选择  情况  在  作为  未来
```

中文母语者很自然会想用 `变量 结果 = ...` / `变量 类型: 字符串` —— **会失败**。改用别名：
`计算结果` / `返回值` / `结果值` / `类型名` / `数据类型`。

> 编译器现在会给出 rustc 风格的友好错误（行列号 + 源码片段 + caret + 「是保留字」提示），看到「`X` 是 qi 的保留字」就换个名。

> 为什么不能降级成「上下文关键字」？因为 `结果<T,E>` / `选项<T>` / `通道<T>` / `未来<T>` 是泛型类型语法，和把这些词当标识符在 `<` 上产生不可消解的 LALR(1) 歧义（`名字<T>` 泛型 vs `名字 < 表达式` 比较）。这是解析器的理论限制。

## 导入系统

```qi
// 标准库导入
导入 标准库.HTTP;
导入 标准库.JSON;
导入 标准库.网络;
导入 标准库.时间;
导入 标准库.输入输出;

// 使用别名
导入 标准库.文本 作为 字符串工具;

// 相对路径导入
导入 ./子模块;
导入 ../工具包;

// 使用点号访问模块函数（不是 :: ）
变量 响应 = HTTP.获取("https://example.com");
```

**重要规则**：
- 模块函数访问用 `.`（如 `HTTP.获取(...)`），也支持 `::` 静态方法形式（如 `字符串::字节长度(...)`）
- 字符串模块**同时注册为** `标准库.字符串` 和 `标准库.文本`，所以 `字符串::字节长度(...)` 和 `文本.查找(...)` 都能用。示例里最常见的是 `字符串::字节长度`、`字符串::子串`、`字符串::包含`
- HTTP 模块是大写 `HTTP`，不是 `http`
- 跨包导入用 destructure：`导入 Web::{创建应用, 启动}`，**不支持** `Web::函数()` 这种跨包静态调用
- `结果` `类型` 等是保留字（见上），不能用作变量名

### 跨包导入示例（使用 qi 写的库）

```qi
导入 Web::{ 创建应用, 启动 };                    // qi-web
导入 CLI::{ 创建应用, 创建命令, 执行 };           // qi-cli
导入 Harness::{ 模型配置, 大模型, 开启会话 };     // qi-harness
```

## 标准库模块

### 标准库.输入输出
```qi
导入 标准库.输入输出 作为 IO;
IO.打印行("你好");
IO.打印("不换行");
变量 内容: 字符串 = IO::读取文件("文件.txt");   // 失败/不存在 → 返回空串 ""
IO::写入文件("文件.txt", "内容");                // 返回 1=成功 / 0=失败
IO::追加文件("文件.txt", "更多");
变量 大小: 整数 = IO::文件大小("文件.txt");       // 不存在 → -1
IO::创建目录("新目录");
```

### ⚠️ FFI 返回值约定（必读，踩坑多）

| 模块/函数 | 成功 | 失败 |
|---|---|---|
| `读取文件` | 文件内容 | **空串 `""`**（不是 NULL；用 `字符串::字节长度(x)==0` 判断失败） |
| 字符串函数（`trim`/`子串`/`替换`…） | 结果 | **空串 `""`** |
| `写入文件`/`追加文件` | `1` | `0` |
| `设置环境变量` | `1` | `-1` |
| `文件大小` | 字节数 | `-1` |
| 多数布尔型 FFI（`包含`/`文件存在`…） | `1` | `0` |

> 返回约定**不完全统一**（有的 1=成功，有的 0=成功）—— 用前查一下，或参考上表。
> 字符串 FFI 失败返回空串而非 NULL，所以 `读取文件(...) + "后缀"` 拼接是安全的（早期版本会被吞成空串，已修复）。

### 通用动词函数名现在可自由使用

`关闭` `打开` `读取` `写入` `长度` `连接` `切片` `比较` 这些**不再被编译器劫持**到标准库 —— 你可以放心定义 `函数 关闭(代理: 整数) { ... }`。文件操作请用**完整名**：`打开文件` / `关闭文件` / `读取文件` / `写入文件`。

### 标准库.HTTP
```qi
导入 标准库.HTTP;
变量 响应 = HTTP.获取("https://api.example.com/data");
变量 结果值 = HTTP.发送("https://api.example.com/data", 数据);
// 服务器
变量 服务器 = HTTP.创建服务器("0.0.0.0", 43510);
```

### 标准库.JSON
```qi
导入 标准库.JSON;
变量 对象 = JSON.创建对象();
JSON.设置字符串(对象, "名字", "Qi");
JSON.设置整数(对象, "版本", 1);
变量 文本 = JSON.转字符串(对象);
```

### 标准库.网络
```qi
导入 标准库.网络;
// TCP
变量 连接 = 网络.TCP连接("127.0.0.1", 6759, 5000);  // 主机, 端口, 超时ms
网络.TCP写入(连接, "你好");
变量 数据 = 网络.TCP读取(连接);
网络.TCP关闭(连接);
// UDP
变量 套接字 = 网络.UDP绑定("0.0.0.0", 6759);
网络.UDP发送到(套接字, "消息", "127.0.0.1", 6760);
```

### 标准库.时间
```qi
导入 标准库.时间;
变量 现在时间 = 时间.现在();
变量 格式化时间 = 时间.格式化(现在时间, "%Y-%m-%d %H:%M:%S");
时间.睡眠毫秒(100);
变量 年份 = 时间.年(现在时间);
```

### 标准库.文本（字符串操作）
```qi
导入 标准库.文本;
变量 位置 = 文本.查找("你好世界", "世界");
变量 子串 = 文本.子串("你好世界", 2, 4);
变量 大写 = 文本.转大写("hello");
变量 包含否 = 文本.包含("你好世界", "你好");
变量 部分 = 文本.分割("a,b,c", ",");
```

### 标准库.加密
```qi
导入 标准库.加密;
变量 哈希值 = 加密.SHA256哈希("数据");
变量 编码值 = 加密.Base64编码("数据");
变量 解码值 = 加密.Base64解码(编码值);
```

### 标准库.JSON（数组操作）
```qi
变量 数组 = JSON.创建数组();
JSON.数组添加字符串(数组, "元素1");
JSON.数组添加整数(数组, 42);
变量 长度 = JSON.数组长度(数组);
变量 值 = JSON.数组获取字符串(数组, 0);
```

### 其他标准库模块
- `标准库.正则` — 正则表达式（是否匹配、查找、替换、切割）
- `标准库.路径` — 路径操作（连接、文件名、父目录、扩展名）
- `标准库.随机` — 随机数（生成整数、浮点、布尔、UUID）
- `标准库.操作系统` — 系统信息、环境变量、目录操作
- `标准库.进程` — 外部命令执行
- `标准库.命令行` — CLI 参数解析
- `标准库.图形化` — 窗口创建、2D渲染、音频播放
- `标准库.列表` — 类型化列表（整数列表、浮点列表、字符串列表）
- `标准库.哈希表` — 类型化哈希表
- `标准库.大模型` — LLM API 调用
- `标准库.WebSocket` — WebSocket 客户端/服务器
- `标准库.MCP服务器` — MCP 协议服务器
- `标准库.数据库` — 数据库连接和查询
- `标准库.压缩` — Gzip 压缩/解压
- `标准库.配置` — TOML/INI 配置文件读写
- `标准库.测试` — 断言和测试框架
- `标准库.环境` — 环境变量操作
- `标准库.向量` — 向量数学运算

## 并发编程

```qi
// 通道通信
变量 信道 = 通道(整数);
启动 {
    信道 <- 42;
}
变量 值 = <- 信道;

// 协程
协程 函数 异步任务() {
    // 异步操作
}

// 选择（select）
选择 {
    情况 值1 = <- 通道1 {
        打印(值1);
    }
    情况 值2 = <- 通道2 {
        打印(值2);
    }
}
```

## CLI 命令

```bash
# 编译并运行
qi run 程序.qi
qi 运行 程序.qi

# 仅编译
qi compile 程序.qi -o 输出
qi 编译 程序.qi -o 输出

# 语法检查
qi check 程序.qi
qi 检查 程序.qi

# 格式化
qi format 程序.qi -i
qi 格式化 程序.qi -i

# 编译器信息
qi info --version
qi 信息 --version
```

## 常见错误与解决

| 错误 | 原因 | 解决 |
|------|------|------|
| `结果` 导致解析失败 | `结果` 是保留字 | 改用 `计算结果`、`返回值` 等 |
| `标准库.http` 找不到 | 模块名大小写敏感 | 用 `标准库.HTTP`（大写） |
| 用 `::` 访问模块函数 | Qi 用 `.` 不用 `::` | 改为 `模块.函数()` |
| `字符串` 模块不存在 | `字符串` 是类型关键字 | 用 `文本` 或 `标准库.文本` |
| TCP连接缺参数 | 需要3个参数 | `TCP连接(主机, 端口, 超时毫秒)` |
| 重复 `_main` 符号 | 多文件都有 `入口()` | 只有入口文件的 `入口()` 变为 `@main` |

## 编写示例代码注意事项

- 端口号不要用常见端口（3000、8000、8080），使用随机高位端口如 `3076`、`6759`、`43510`
- 硬编码的地址/端口应提取为变量
- 打印信息使用 `打印行()` 而不是 `print()`（`print` 是保留字但打印行是标准函数）
- 所有示例需要 `包 主程序;` 开头
- 入口函数为 `函数 入口()` 而非 `函数 主函数()`
- 注释使用 `//`，与 C/Rust 相同

## 完整示例：HTTP 客户端

```qi
包 主程序;

导入 标准库.HTTP;

函数 入口() {
    打印行("=== HTTP 客户端示例 ===");

    变量 网址 = "https://httpbin.org/get";
    打印行("请求地址: " + 网址);

    变量 响应 = HTTP.获取(网址);
    打印行("响应内容:");
    打印行(响应);
}
```

## 完整示例：JSON 处理

```qi
包 主程序;

导入 标准库.JSON;

函数 入口() {
    变量 用户 = JSON.创建对象();
    JSON.设置字符串(用户, "姓名", "张三");
    JSON.设置整数(用户, "年龄", 30);
    JSON.设置布尔(用户, "活跃", 1);

    变量 地址 = JSON.创建对象();
    JSON.设置字符串(地址, "城市", "北京");
    JSON.设置对象(用户, "地址", 地址);

    变量 文本 = JSON.格式化(用户);
    打印行(文本);

    JSON.删除(地址);
    JSON.删除(用户);
}
```

## 完整示例：排序算法

```qi
包 主程序;

函数 冒泡排序(数组: 数组, 长度: 整数) {
    变量 i = 0;
    当 i < 长度 - 1 {
        变量 j = 0;
        当 j < 长度 - 1 - i {
            如果 数组[j] > 数组[j + 1] {
                变量 临时 = 数组[j];
                数组[j] = 数组[j + 1];
                数组[j + 1] = 临时;
            }
            j = j + 1;
        }
        i = i + 1;
    }
}

函数 入口() {
    变量 数据 = [64, 34, 25, 12, 22, 11, 90];
    冒泡排序(数据, 7);
    打印行("排序完成");
}
```
