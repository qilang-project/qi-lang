---
name: qi-lang
description: Write, understand, and debug Qi (奇语) programming language code — a compiled language with 100% Chinese keywords, LLVM native backend, ARC automatic memory management (自动引用计数), full exception handling (尝试/捕获/最终/抛出), goroutine-style concurrency (启动/通道/未来), a package manager (qi.toml / qi get), and Linux cross-compilation. Use when the user asks to write Qi code, create .qi files, fix Qi compile errors, use the Qi standard library (HTTP, JSON, 网络, 加密, 哈希表, 子进程 etc.), organize multi-file Qi projects, add dependencies, run qi test, or cross-compile Qi programs.
metadata:
  author: qilang
  version: "1.0"
---

# Qi 语言 (奇语)

Qi（奇语）是一门 100% 中文关键字的编译型语言，经 LLVM（inkwell 后端，LLVM 21）编译为原生可执行文件。当前版本 2026.07.05-1，`/usr/local/bin/qi` 解压即用（`bin/` + `lib/qi/` 布局）。内存管理为 ARC（自动引用计数，默认开启）。

> 相关技能：`qi-web`（Web 框架）、`qi-cli`（命令行框架）、`qi-harness`（LLM Agent 框架）—— 均为 Qi 写的第三方包，用它们时先读对应技能。

## 何时使用此技能

- 编写 `.qi` 文件、理解/调试/修复 Qi 程序或编译错误
- 使用 Qi 标准库（HTTP、JSON、网络、加密、哈希表、子进程等）
- 组织多文件 Qi 项目、配置 qi.toml 依赖、`qi get` 拉包、`qi test` 跑测试
- 交叉编译 Linux 部署产物、排查内存（ARC）行为

## 程序结构速查

可执行程序**必须** `包 主程序;` 开头 + `函数 入口()` 入口（库包用自己的包名、不写入口）：

```qi
包 主程序;

导入 标准库.文本;

函数 加法(x: 整数, y: 整数): 整数 {
    返回 x + y;
}

函数 入口() {
    变量 名字 = "Qi";              // 类型推断；常量 用 常量
    变量 年龄: 整数 = 10;          // 显式类型
    如果 年龄 > 5 {
        打印行("你好，" + 名字 + "！结果=" + 加法(1, 2));
    } 否则 {
        打印行("小");
    }
    对于 x 在 [1, 2, 3] {          // 只能遍历数组，无 0..10 区间
        打印行(x);
    }
}
```

基础类型：`整数`(i64)、`浮点数`(f64)、`字符串`、`布尔`(真/假)、`空`。字符串拼接用 `+`（可直接拼数字）。类型转换用内建函数：`整数转字符串` `浮点数转字符串` `字符串转整数` `字符串转浮点数` `整数转浮点数` `浮点数转整数`。

## 高频坑清单（全部实测确认，2026.07.05-1）

1. **`匹配`/`选择`/`跳出`/`继续` 已可用**（2026.07.03 起，此前静默无效）。匹配支持字面量分支/守卫（`k 如果 k > 100 =>`）/`_` 通配/变量绑定；裸块即语句（分号可选），全分支返回时函数尾无需多余返回；只能作语句不能作表达式；不支持的模式（结构体/或模式 `|`）会编译报错。选择 支持 `默认`/`超时(毫秒)`。循环外 `跳出` 现在是编译错误。`循环 {}` 仍无语法。详见 references/语法参考.md。
2. **泛型可用**（2026.07.05-1 起）：`函数 最大<T>(a: T, b: T): T`（调用点推断，全 T 位类型须一致）、`枚举 盒装<T>`、`类型 对<T>`（单态化，≤2 类型参数，嵌套 ≤8 层）。**显式类型实参是 turbofish**：`最大::<整数>(...)`（`最大<整数>(...)` 是语法错误，LR(1) 与比较歧义）；泛型结构体字面量必须 `新建 对<整数> { ... }`；泛型枚举构造带模板名 `盒装.满(x)`。`选项<T>{有(T),无}`/`结果<T>{成(T),败(字符串)}` 内建，裸构造子 有/无/成/败 四级定型（返回类型→变量注解→形参→实参反推）。**通道<字符串>/<浮点数>/<布尔> 解锁**（此前只有整数），发送 `信道 <- 值`、接收 `<-信道`。`作为` 只能转基础类型。
2. **枚举可用**（2026.07.04-2 起）：`枚举 颜色 { 红, 绿, 蓝 }`（无载荷=整数 tag，`==` 可比）、`枚举 形状 { 圆(浮点数), 点 }`（带载荷装箱，ARC 回收）；构造 `颜色.红`/`形状.圆(3.14)`；匹配解构 `圆(半径) =>` 可配守卫；**穷尽性编译期强制**（缺分支报错，`_` 兜底豁免）。变体名不能用保留字（`空`→`无内容`）。`枚举` 成保留字。
2. **2026.07.04 起已修复**：转义字符字面量（`'\n' '\t' '\\' '\'' '\0'` 全可用）；块注释可嵌套（`/* a /* b */ c */`）；比较/算术关键字可作 `模块.方法` 名（`文本.等于`、`向量.加` 解锁）；`标准库.向量` 六函数端到端可用（此前注册表与 FFI 全线错位，从未工作过）。
3. **区间循环双形式**（2026.07.04 起）：`对于 i 在 0..10 {}` 半开（不含 10）、`对于 i 在 0 到 9 {}` 闭区间（含 9，中文语感）——两种都循环 10 次；起止为任意整数表达式（进循环前各求值一次）。`到` 为保留字。嵌套泛型 `数组<数组<整数>>` 的 `>>` 现在会被当右移，需写空格 `> >`。
4. **数组下标赋值可用**（2026.07.04 起）：`甲[i] = 表达式;`（RC 元素自动 retain 新/release 旧，自赋值安全）；链式 `甲[i][j] = x` 暂不支持（先取行再赋）。位运算 `& | ^ << >>` 同批可用（仅整数，`>>` 算术右移，优先级 Go 风格）；比较结果可直接当整数用（真=1 假=0）。
5. **结构体字面量必须括号包裹或用 新建**：`变量 p = (点 { x: 1, y: 2 });` 或 `新建 点 { x: 1, y: 2 }`。裸 `点 { }` 解析报错。
6. **else-if 链是怪语法** `} 否则如果 如果 x > 5 {`（否则如果 后还要再写 如果）。推荐嵌套：`否则 { 如果 ... }`。
7. **`打印行` 逗号多参会逐参各占一行**（且并发下可与其他协程交错）。永远用 `+` 拼成单串再打印。
8. **`作为` 类型转换不生成代码**（`f 作为 整数` 报「初值无值」），用上面六个转换内建。`f"..."` 插值同样 codegen 不支持，用 `+` 拼接。
9. **模块别名后字符串返回类型会丢失**：`导入 标准库.输入输出 作为 IO;` 后 `变量 内容 = IO.读取文件(...)` 打印出指针数字——给变量加显式 `: 字符串` 标注，或不用别名。
10. **无注解竖线闭包返回类型默认 空**：`|x: 整数| { 返回 x+1; }` 传给 `函数(整数):整数` 形参会得垃圾值——写返回注解或用完整语法 `闭包(x: 整数): 整数 {}`。竖线闭包参数不接受默认值（与位或歧义）。
10. ~~比较表达式不能直接当整数实参~~（2026.07.04 起已修：i1 自动加宽为整数）：`断言(x > 0, ...)` 生成 i1 vs i64 LLVM 校验失败——先 `如果` 分支再传 `1`/`0`。
10. **浮点数组字面量元素必须写 `1.0`**：`[1, 2, 3]` 推成整数数组，传给 `向量.*` 是垃圾位模式；写 `[1.0, 2.0, 3.0]`。
11. **保留字地雷**：`结果 类型 数组 列表 字典 集合 任务 循环 选项 通道 未来 乘 除 加 减` 等都不能做变量名（报错会提示换名，如 `数组`→`数列`）。`长度` 例外可用；`列表` 等类型关键字做模块名也不行——`导入 标准库.列表 作为 列表库;`。
12. **`文本.子串` 按字节索引**，切在多字节字符中间会直接 panic。中文 3 字节/字，先用 `字节长度`/`字符数量` 算好边界。
13. **端口规范**：示例严禁 8080/3000/8000，用 3000 以上随机高位如 `6759`、`43719`、`46271`。
14. 同目录同包的无导入文件会被**自动并入编译**（详见包管理参考）；含 `导入` 的文件必须显式 `导入 ./文件名;`。

## 深入阅读路由

| 要做什么 | 读哪个文件 |
|---|---|
| 类型/控制流/函数(默认参数/变参)/结构体与方法/闭包/异常/字符串/数组/保留字 | [references/语法参考.md](references/语法参考.md) |
| 标准库各模块 API（输入输出/文本/JSON/HTTP/网络/时间/加密/哈希表/子进程…）与返回值约定 | [references/标准库.md](references/标准库.md) |
| 启动(goroutine)/通道/未来<T>与等待/协程异常/同步原语(锁/原子/等待组) | [references/并发异步.md](references/并发异步.md) |
| CLI 命令与中文别名/-O 优化/交叉编译/qi test/qifmt/ARC 与 QI_ARC/QI_RC_REPORT | [references/工具链.md](references/工具链.md) |
| qi.toml 依赖三种写法/qi get 与缓存/qi.lock/导入解析顺序/多文件项目组织 | [references/包管理.md](references/包管理.md) |

## CLI 速查

```bash
qi run 程序.qi          # 编译并运行（别名 qi 运行）
qi check 程序.qi        # 只查语法（别名 检查）
qi compile 程序.qi -o 出  # 编译（别名 编译）；-O maximum 最高优化
qi test .              # 发现并运行 *_测.qi（别名 测试）
qi get                 # 拉取 qi.toml 远程依赖（别名 拉取）
qifmt 文件.qi           # 格式化（只动空白；qi format 是未实现的桩）
```
