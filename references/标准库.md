# Qi 标准库参考

权威来源为编译器注册表 `qi/src/codegen/module_registry.rs`（约 30+ 模块、500+ 函数）；本文挑每模块高频核心 API。代码块均实测通过（2026.07.03-1）。

通用规则：

- 导入 `导入 标准库.模块名;`，调用 `模块名.函数(...)`。
- 模块几乎都以「裸名 + `标准库.` 前缀」双注册。别名映射：字符串→**文本**、时间→日期、操作系统→OS、大模型→LLM。
- `列表`/`数组`/`字典`/`集合` 是类型关键字，对应模块**必须**起别名：`导入 标准库.列表 作为 列表库;`。
- 用户 `作为` 别名后，返回字符串的调用赋给变量要加 `: 字符串` 标注（类型推断丢失，打印会出指针数字）。
- **没有 `标准库.数学`**——`数学.最大值` 之类来自第三方包，标准库里做算术直接用运算符。

## 返回值约定（FFI 踩坑表）

| 函数族 | 成功 | 失败 |
|---|---|---|
| `读取文件`、字符串函数(去空白/替换…) | 结果 | **空串 ""**（用 `文本.字节长度(x) == 0` 判断） |
| `写入文件`/`追加文件`/`删除文件`/目录操作 | `1` | `0` |
| 布尔型（`包含`/`文件存在`/`是否匹配`/`存在`…） | `1`(真) | `0`(假) |
| `文本.查找` | 字节位置 | `-1` |
| `文件大小` | 字节数 | `-1` |
| HTTP 方法 | 响应体 | 内容为 `"HTTP错误: ..."` 的字符串 |
| 句柄构造（JSON/列表/哈希表/服务器/子进程…） | 整数句柄（后续操作首参） | — |

约定不完全统一，用前对表。

## 输入输出（文件与打印）

```qi
导入 标准库.输入输出;

输入输出.写入文件("/tmp/演示.txt", "第一行\n");   // 1=成功
输入输出.追加文件("/tmp/演示.txt", "第二行\n");
变量 内容 = 输入输出.读取文件("/tmp/演示.txt");   // 失败返回 ""
打印行(内容);
打印行(输入输出.文件存在("/tmp/演示.txt"));       // 1
打印行(输入输出.文件大小("/tmp/演示.txt"));       // 字节数；不存在 -1
输入输出.删除文件("/tmp/演示.txt");
```

另有：`创建文件` `创建目录` `删除目录` `创建符号链接`。`打印`/`打印行` 是全局内建，无需导入。

## 文本（字符串操作）

```qi
导入 标准库.文本;

变量 s = "  你好，Qi 世界  ";
变量 干净 = 文本.去空白(s);
打印行(文本.字节长度(干净));        // UTF-8 字节数
打印行(文本.字符数量(干净));        // 字符数
打印行(文本.包含(干净, "世界"));    // 1
打印行(文本.替换(干净, "世界", "宇宙"));
打印行(文本.转大写("hello"));       // HELLO；转小写 同理
打印行(文本.查找(干净, "Qi"));      // 字节位置；未找到 -1
打印行(文本.子串("你好世界", 0, 6)); // 你好 —— 按字节！切错边界 panic
```

`分割` 返回**字符串列表句柄**，配合列表模块读取：

```qi
导入 标准库.文本;
导入 标准库.列表 作为 列表库;

变量 句柄 = 文本.分割("苹果,香蕉,樱桃", ",");
变量 个数 = 列表库.字符串列表大小(句柄);
变量 i = 0;
当 i < 个数 {
    打印行(列表库.获取字符串(句柄, i));
    i = i + 1;
}
```

另有：`开始于` `结束于` `等于` `查找从位置` `子串从位置`。

## JSON

句柄式 API。`创建对象/创建数组` 返回句柄，用完 `删除` 释放：

```qi
导入 标准库.JSON;

变量 用户 = JSON.创建对象();
JSON.设置字符串(用户, "姓名", "张三");
JSON.设置整数(用户, "年龄", 30);
JSON.设置布尔(用户, "活跃", 1);
变量 标签 = JSON.创建数组();
JSON.数组添加字符串(标签, "工程师");
JSON.设置数组(用户, "标签", 标签);
打印行(JSON.转字符串(用户));   // {"姓名":"张三","年龄":30,"活跃":true,"标签":["工程师"]}
// JSON.格式化(句柄) 为带缩进版本

变量 句柄 = JSON.解码("{\"城市\":\"北京\",\"人口\":2154}");  // 解析
打印行(JSON.获取字符串(句柄, "城市"));
打印行(JSON.获取整数(句柄, "人口"));
JSON.删除(句柄);
JSON.删除(用户);
```

另有：`设置浮点数/对象` `获取浮点数/布尔/对象/数组` `是否包含键` `数组长度` `数组添加整数/浮点数/布尔/对象` `数组获取字符串/整数/…`。拼 JSON 一律用此 API 构建，别手拼字符串（引号转义易碎）。

## HTTP

客户端（reqwest 实现，真实网络）：

```qi
导入 标准库.HTTP;

变量 响应: 字符串 = HTTP.获取("https://example.com");        // GET
变量 回执 = HTTP.发送("https://api.example.com", "{\"a\":1}"); // POST(URL, 体)
// 更新=PUT 删除=DELETE 修补=PATCH；请求(方法, URL, 头, 体) 全定制
```

服务器（简单阻塞式；生产用 qi-web 框架）：

```qi
导入 标准库.HTTP;

常量 主机 = "127.0.0.1";
常量 端口: 整数 = 46271;    // 用 3000 以上随机高位端口，严禁 8080/3000/8000

函数 入口() {
    变量 服务器 = HTTP.创建服务器(主机, 端口);
    变量 请求 = HTTP.接受连接(服务器);   // 阻塞，返回原始请求文本
    打印行(请求);
    HTTP.关闭服务器(服务器);
}
```

## 网络（TCP/UDP）

```
TCP连接(主机, 端口, 超时ms) -> 句柄        TCP读取(句柄, 缓冲区大小) -> 字符串
TCP写入(句柄, 字符串) -> 写入字节数        TCP关闭(句柄)
TCP监听(主机, 端口, 队列) -> 句柄          TCP接受连接(句柄) -> 客户端句柄
UDP绑定(主机, 端口) -> 句柄               UDP发送到(句柄, 消息, 目标主机, 端口)
UDP接收(句柄, 缓冲区) -> 字符串           端口可用(端口) -> 1/0
获取本机IP() -> 字符串                    解析主机(域名) -> IP
```

注意 `TCP读取` 是两参（句柄、缓冲区大小），旧文档单参写法已过时。

## 时间

```qi
导入 标准库.时间;

变量 现在时间 = 时间.现在();                                  // Unix 秒
打印行(时间.格式化(现在时间, "%Y-%m-%d %H:%M:%S"));
打印行(时间.年(现在时间));    // 月/日/时/分/秒/星期几 同理
时间.睡眠毫秒(50);
// 现在毫秒() 当前毫秒；解析(文本, 格式)；加秒/加分钟/加小时/加天/加周
```

## 哈希表（键为字符串，按值类型分族）

```qi
导入 标准库.哈希表;

变量 年龄表 = 哈希表.创建整数表();
哈希表.设置整数(年龄表, "张三", 25);
打印行(哈希表.获取整数(年龄表, "张三"));   // 25
打印行(哈希表.包含键(年龄表, "王五"));     // 0
打印行(哈希表.表大小(年龄表));
哈希表.释放表(年龄表);
// 字符串值：创建字符串表/设置字符串/获取字符串；浮点：创建浮点表/…
// 另有 删除键 清空表
```

## 列表（可变序列，替代数组下标赋值）

```qi
导入 标准库.列表 作为 列表库;   // 必须别名

变量 表 = 列表库.创建整数列表();
列表库.添加整数(表, 64);
列表库.设置整数(表, 0, 99);          // 下标写！数组字面量做不到的
打印行(列表库.获取整数(表, 0));
打印行(列表库.整数列表大小(表));
列表库.删除列表(表);
// 弹出整数/插入整数/删除整数元素/包含整数/查找整数索引/清空整数列表
// 字符串族：创建字符串列表/添加字符串/获取字符串/字符串列表大小；浮点族同理
```

## 随机

```qi
导入 标准库.随机;

变量 n = 随机.生成整数(1, 100);      // [最小, 最大]
打印行(随机.UUID());                 // 标准 UUID 字符串
// 生成浮点(最小, 最大) 生成布尔() 生成字符串(长度)
```

## 加密

```qi
导入 标准库.加密;

打印行(加密.SHA256哈希("你好"));       // 十六进制摘要；SHA512哈希/MD5哈希 同理
变量 码 = 加密.Base64编码("你好 Qi");
打印行(加密.Base64解码(码));
// HMAC_SHA256(数据, 密钥)
```

## 正则

**模式在前，文本在后**：

```qi
导入 标准库.正则;

打印行(正则.是否匹配("\\d+", "订单号 12345"));      // 1
打印行(正则.查找("\\d+", "订单号 12345"));          // 12345
打印行(正则.全部替换("\\d", "订单号 12345", "*"));  // 订单号 *****
// 查找全部/切割 返回 JSON 数组字符串
```

## 路径

```qi
导入 标准库.路径;

变量 全 = 路径.连接("/tmp", "数据.json");
打印行(路径.文件名(全));    // 数据.json
打印行(路径.扩展名(全));    // json
打印行(路径.父目录(全));    // /tmp
打印行(路径.存在("/tmp"));  // 1；是目录/是文件/绝对路径 同理
```

## 操作系统

```qi
导入 标准库.操作系统;

打印行(操作系统.操作系统类型());   // macos / linux / windows
打印行(操作系统.CPU核心数());
操作系统.设置环境变量("KEY", "值");
打印行(操作系统.获取环境变量("KEY"));
打印行(操作系统.当前目录());
// 系统架构/主机名/用户名/用户主目录/临时目录/切换目录/进程ID/退出程序(码)
// 列出目录/是否为目录/是否为文件/加载环境文件(.env)
```

## 子进程

```qi
导入 标准库.子进程;

变量 句柄 = 子进程.生成("cat", "[]");        // (命令, 参数JSON数组) -> 句柄
子进程.写入行(句柄, "回显测试");
变量 行 = 子进程.读取行超时(句柄, 2000);      // (句柄, 超时ms)
打印行("收到: " + 行);
子进程.结束(句柄);
// 读取行(句柄) 阻塞版；存活(句柄) -> 1/0
```

## 测试（断言，配合 qi test）

```qi
导入 标准库.测试;

函数 入口() {
    测试.断言相等_整数(1 + 1, 2, "一加一等于二");   // (实际, 期望, 消息)
    测试.断言真(1, "真值断言");
    测试.断言相等_字符串("你好", "你好", "字符串相等");
    // 断言相等_浮点/断言不等_整数/断言假/测试通过(消息)/测试失败(名, 详情)
}
```

文件命名 `*_测.qi` 即被 `qi test` 发现。更完善的用例/桩框架见第三方包 qi-test（`导入 测试::{运行用例, 摘要并退出, ...}`）。

## 其他模块（按需查注册表）

## 向量（数值计算，2026.07.04 起可用）

```qi
导入 标准库.向量;
// 元素必须写浮点字面量(1.0),整数字面量会推成整数数组 → FFI 读到垃圾
变量 甲 = [1.0, 2.0, 3.0];
变量 乙 = [4.0, 5.0, 6.0];
打印行("点积 = " + 浮点数转字符串(向量.点积(甲, 乙)));      // 32
变量 和向量 = 向量.加(甲, 乙);                              // [5,7,9] 新数组,ARC 回收
打印行("模 = " + 浮点数转字符串(向量.长度([3.0, 4.0])));    // 5
变量 单位 = 向量.归一化([3.0, 4.0]);                        // [0.6, 0.8]
变量 倍 = 向量.数乘(甲, 2.0);
打印行(浮点数转字符串(向量.余弦相似度(甲, 甲)));            // 1(平行)
```

维度不匹配/坏输入返回 0.0 或空数组(宁保守不崩,一次性 [qi-vector] 日志)。

`标准库.WebSocket`、`标准库.MCP服务器`/`MCP客户端`、`标准库.数据库`(SQLite)、`标准库.压缩`(Gzip)、`标准库.配置`(TOML/INI)、`标准库.大模型`(LLM API)、`标准库.命令行`(clap 风格 CLI 构建器)、`标准库.进程`、`标准库.环境`、`标准库.图形化`(GUI)、`标准库.同步`(见并发参考)、`标准库.TLS`、`标准库.信号`、`标准库.字节切片`。
